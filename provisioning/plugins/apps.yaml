apiVersion: 1

# Enable the ECharts app plugin (which bundles the nested panels) in the dev
# server. App plugins are disabled by default and must be explicitly enabled.
apps:
  - type: grafana-echarts-app
    org_id: 1
    disabled: false
